<% @results.each do |instagram| %>
  <div class = "image-display">
    <div class = "overlay">
      <%= image_tag(instagram.images.standard_resolution.url, class: "tagged-images") %>
        <%= link_to(instagram.images.standard_resolution.url, class: "test-popup-link") do %>
        <div class = "image-info">
          <div class = "image-info-placement">
            <%= if instagram.caption != nil
                instagram.caption.text
              end %>
              <br/>
          <strong><%= instagram.user.username %></strong> - 
          <%= instagram.likes[:count] %> Likes
          </div>
        </div>
        <% end %>
    </div>
  </div>
<% end %>
<%= will_paginate @results %>

          instagram.caption.text
          by
          <strong>instagram.user.username</strong> - 
          instagram.likes[:count] Likes

<% @results.each do |instagram| %>
<% title_text = '<p>' + instagram.caption.text.to_s + '</p>' +' by ' + '<strong>'+ instagram.user.username.to_s + '</strong>' + ' ' + instagram.likes[:count].to_s + ' Likes' %>
  <div class = "image-display">
    <div class = "overlay">
      <%= link_to image_tag(instagram.images.standard_resolution.url, class: "tagged-images fancybox"), instagram.images.standard_resolution.url, :data => { :colorbox => true, :colorbox_title => title_text} %>
    </div>
  </div>
<% end %>
<%= will_paginate @results %>
